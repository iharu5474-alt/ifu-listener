import { Playlist, Track } from '../types';

/**
 * ifu listener starts completely empty.
 * Users search for tracks and create their own custom playlists from scratch.
 */
export const INITIAL_FEATURED_TRACKS: Track[] = [];

export const CURATED_PLAYLISTS: Playlist[] = [];
